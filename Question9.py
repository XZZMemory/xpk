'''本题要求编写程序，计算交错序列 1-2/3+3/5-4/7+5/9-6/11+... 的前N项之和。

输入格式:
输入在一行中给出一个正整数N。

输出格式:
在一行中输出部分和的值，结果保留三位小数。'''
n = int(input())
i = 1
sum = 0
while i <= n:
    if i & 1 == 1:
        sum += (i / (2 * i - 1))
    else:
        sum -= (i / (2 * i - 1))
    i = i + 1
print("{:.3f}".format(sum))
